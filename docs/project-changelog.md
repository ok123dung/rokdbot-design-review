# Project Changelog — rokdbot.com

Living record of significant changes, features, and fixes.

## 2026-05-11 — 204 Articles + Code-Split Performance

### Features
- **204 SEO articles total** (180 mới trong session, từ 24 đầu phiên):
  - 24 original (3 legacy + 8 feature + 5 funnel + 5 angles + 3 bonus)
  - 30 Tier 1 (commander + KvK + building + events + F2P + server)
  - 50 Tier 2 (deeper commander/KvK/resource/troop/F2P/PvP/misc)
  - 20 commander individual deep-dives (Mehmed II, Yi Seong-Gye, Lohar, etc.)
  - 10 pet system + KvK season specific (Iron Wolf, Hawk, KvK 1-4)
  - 30 events + bot features + strategy/buyer guides
  - 20 more commanders + city/troops/equipment
  - 30 commanders + city + buyer questions + seasonal events
- **Sitemap.xml** 207 URLs (204 articles + 3 pages)
- **Categorized BlogList** với 4 sections + cover thumbnails

### Performance
- **Code-split blogData**: from 813 KB monolithic chunk → 53 KB metadata + 5-13 KB per-article chunks
- 52% reduction in initial bundle load
- `import.meta.glob` lazy-load per slug
- New `blogMeta.ts` (auto-generated by `scripts/gen-blog-meta.py`)
- Deprecated `blogData.ts` (now stub for type re-export only)

### SEO Schema
- BreadcrumbList schema cho `/blog` + `/blog/<slug>`
- WebSite schema (homepage) → Google Sitelinks Search Box
- Table of Contents (auto-generated từ H2 với Vietnamese-aware slugify)
- ISODate brand type + runtime guard

### Tools
- `scripts/gen-blog-meta.py` — auto-regenerate blogMeta.ts từ posts/*.ts

## 2026-05-09 — SEO Blog Cluster + Premium VIP Tier

### Features
- **8 SEO blog articles per RokdBot feature** in case-study + benchmark style
  - `combo-spam-luring-aoe-rokdbot-v2` — V2 độc quyền, 1 đạo chain technique
  - `auto-honor-farming-kvk-rok-2026` — case study 17 tháng FREE AP customer (Power 98M, 161k Victory)
  - `auto-rally-captain-rok-2026` — alliance rally pháo đài + sanctum + holy site
  - `auto-build-research-rok-vip-template` — V1 entry, City Hall lv 25
  - `auto-farm-4-rss-tile-level-5-rok` — 4 march parallel + gem mining 8.000/ngày
  - `ai-commander-rotation-v3-rok` — V3 độc quyền, skip 6 tháng farm expertise
  - `multi-account-sync-rokdbot-v3` — V3 độc quyền, cloud sync 2-3 acc
  - `anti-captcha-2captcha-rok-bot-2026` — V3+ feature, 2Captcha API integration
- **Premium VIP 3M tier** added vào Supabase `service_packages` (tier #5)
- **5-column package grid** trên homepage `/#packages` (`pkg-grid-v3`)
- **Markdown renderer upgrade** trong `BlogPost.tsx`: support `[link](url)`, `**bold**` inline, `![image](url)`, `> blockquote` CTA box
- **Custom hero image** per article (`coverImage` field rendered above content)

### Fixes
- **C1**: `BlogPost.tsx` table renderer hardcoded `grid-cols-4` → broken cho 3/5/6-col tables. Fixed: dynamic `gridTemplateColumns: repeat(N, 1fr)` based on cell count.
- **C3**: No slug uniqueness check → silent collision (article unreachable). Fixed: build-time assertion in `blogData.ts`.
- **H7**: `PackageGrid.tsx` `JSON.parse` could crash silently if DB row malformed. Fixed: inner try/catch returns `null`.

### Refactor
- **Split `blogData.ts`** từ 1.371 lines → 36-line aggregator + 11 per-article files trong `src/pages/blog/posts/`
- **Branded `ISODate` type** trong `src/pages/blog/blogTypes.ts` + runtime ISO format check
- **Tier-aware CTA** per article (V1/V2/V3/Premium VIP)
- **Cross-link cluster** — mỗi article link 2-3 article khác trong cluster

## 2026-05-08 — Site rescue + V3 redesign deploy

### Critical Fix
- **Blank page rescue** rokdbot.com — broken bundle với `supabaseUrl is required` error
  - Root cause: `VITE_SUPABASE_URL` + `VITE_SUPABASE_PUBLISHABLE_KEY` chưa set trong Vercel project env
  - Fix: env vars set + rebuild với env baked + CLI prebuilt deploy
- **SW kill switch deployed** at `/sw.js` để rescue browsers với stale SW cache (commit `06ea4d3`)

### Deploy
- **V3 Redesign merged** từ branch `redesign/v3` (commit `14cbc86`) → main
  - Aurora design system
  - Bento 12 features grid
  - Live Dashboard hero (variant B)
  - Stats Wall 6 numbers
  - Be Vietnam Pro typography (replaces Orbitron cho VN diacritic)

### Infrastructure
- **Vercel deploy workflow documented**: `framework: null` blocks git auto-deploy → must use CLI prebuilt + `vercel promote` for custom domain alias swap

## 2026-04-25 — Production redirect to working bundle

- Last known good production: `dpl_FUTYYvANFwChY6hr6KtdrmebZmdo` (commit `9cd76543`, port SEO + v2 design tokens)
